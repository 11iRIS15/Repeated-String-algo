# Repeated-String-algo
This function counts how many times the letter 'a' appears in the first n characters of an infinitely repeated string s. It first calculates the 'a's in the full repetitions of s within n, then adds the 'a's from the remaining partial repetition at the end.
